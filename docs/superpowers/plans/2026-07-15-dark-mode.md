# Dark Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar um toggle de tema claro/escuro ao layout do sistema, persistido em
`localStorage`, sem quebrar nenhuma tela existente.

**Architecture:** Os tokens de cor já são CSS custom properties (Tailwind v4 `@theme`) que as
utilities referenciam via `var(--color-*)`. Um `:root[data-theme="dark"]` override no CSS
redefine essas variáveis; um `ThemeContext` aplica o atributo `data-theme` no `<html>` e persiste
a escolha. Dois problemas do código atual (auditados via grep, não hipotéticos) são corrigidos
como parte deste trabalho: `bg-white` hardcoded em 6 arquivos, e o papel duplo de
`text-primary-dark`/`text-accent-dark` (fundo-escuro vs. texto-sobre-soft).

**Tech Stack:** React + TypeScript + Tailwind CSS v4. Sem framework de testes automatizados
(convenção já estabelecida no projeto) — verificação manual via `npx tsc -b --noEmit` e navegador.

## Global Constraints

- Só toggle claro/escuro — sem opção "seguir o sistema".
- Preferência em `localStorage`, chave `saas_theme`, valores `"light"` | `"dark"`.
- Padrão sem preferência salva: `"light"`.
- Toggle só na Navbar (não aparece em Login/Register, mas essas telas respeitam a preferência já
  salva via o atributo `data-theme` no `<html>`).
- Os tokens `--color-primary-dark`/`--color-accent-dark` continuam servindo só pro papel de
  fundo/hover escuro (Navbar, painel de login, hover de botão) — nunca mais usados como cor de
  texto depois deste plano.

---

### Task 1: Paleta escura no CSS

**Files:**
- Modify: `frontend/src/index.css`

**Interfaces:**
- Produces: os tokens `--color-surface`, `--color-primary-soft-text`, `--color-accent-soft-text`
  (valores claros, no `@theme`) e o bloco `:root[data-theme="dark"]` com todos os overrides —
  consumidos por todas as tasks seguintes.

- [ ] **Step 1: Substituir o conteúdo de `frontend/src/index.css`**

Substituir o arquivo inteiro por:

```css
@import "tailwindcss";

@theme {
  /* Paper: pale sage-tinted neutral (not the cliché warm cream) — echoes the pine accent */
  --color-paper: #f5f6f1;
  --color-paper-2: #ececE4;
  --color-ink: #16211d;
  --color-ink-soft: #4b564f;
  --color-ink-faint: #8b9289;

  /* Pine/teal — primary brand color, distinct from generic SaaS indigo/blue */
  --color-primary: #1f6f5c;
  --color-primary-dark: #12463a;
  --color-primary-soft: #dceae5;
  --color-primary-soft-text: #12463a;

  /* Terracotta — sparing accent for the signature price-tag notch + highlights */
  --color-accent: #e8734a;
  --color-accent-dark: #c25a35;
  --color-accent-soft: #fbe4d8;
  --color-accent-soft-text: #c25a35;

  --color-danger: #c1443a;
  --color-danger-soft: #f8dedc;

  --color-line: #dedcd2;
  --color-surface: #ffffff;

  --font-display: "Space Grotesk", sans-serif;
  --font-body: "IBM Plex Sans", sans-serif;
  --font-mono: "IBM Plex Mono", monospace;
}

:root[data-theme="dark"] {
  color-scheme: dark;

  --color-paper: #141d19;
  --color-paper-2: #1b2620;
  --color-ink: #edf1ec;
  --color-ink-soft: #a3ada5;
  --color-ink-faint: #6d766e;

  --color-primary: #3f9c82;
  --color-primary-dark: #0d3229;
  --color-primary-soft: #1c3a32;
  --color-primary-soft-text: #a7e0cd;

  --color-accent: #ea7a52;
  --color-accent-dark: #c9683f;
  --color-accent-soft: #3a231b;
  --color-accent-soft-text: #f3b596;

  --color-danger: #e0655a;
  --color-danger-soft: #3a201c;

  --color-line: #2b3934;
  --color-surface: #1c2723;
}

@layer base {
  html {
    color-scheme: light;
  }

  body {
    margin: 0;
    background-color: var(--color-paper);
    color: var(--color-ink);
    font-family: var(--font-body);
    -webkit-font-smoothing: antialiased;
  }

  h1, h2, h3, h4 {
    font-family: var(--font-display);
    letter-spacing: -0.01em;
  }

  :focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }
}

@layer utilities {
  /* Recorte de "etiqueta de preço" — o elemento assinatura do catálogo.
     Não define `position` aqui: o componente já aplica a utility `absolute`
     do Tailwind, e como ambas as classes vivem na mesma @layer, a que
     definir `position` por último venceria a cascata. */
  .price-tag {
    clip-path: polygon(10px 0, 100% 0, 100% 100%, 10px 100%, 0 50%);
  }
}
```

O bloco `:root[data-theme="dark"]` fica **fora** de qualquer `@layer` de propósito — assim como o
`:root` que o `@theme` gera automaticamente também não é "layered", isso garante que o override
do tema escuro vença o cascade sem depender de guerra de especificidade com `@layer base`.

- [ ] **Step 2: Verificar compilação**

```bash
cd frontend
npx tsc -b --noEmit
```

Esperado: nenhuma saída (CSS não afeta o tsc, mas confirma que nada mais quebrou).

- [ ] **Step 3: Verificar visualmente com um toggle manual**

Como o `ThemeContext` ainda não existe (Task 2), a verificação aqui é manual via DevTools:

1. Rodar `npm run dev` no frontend, abrir o app no navegador, logar.
2. No console do DevTools, rodar: `document.documentElement.setAttribute('data-theme', 'dark')`.
3. Confirmar que o fundo da página escurece e o texto principal fica claro.
4. **Esperado (não é bug):** cards, inputs, modal e bolha de chat continuam com fundo branco
   chapado — isso só é corrigido na Task 4. O objetivo aqui é só confirmar que as variáveis CSS
   trocam de valor corretamente.
5. Rodar `document.documentElement.removeAttribute('data-theme')` pra voltar ao claro e confirmar
   que nada mudou em relação ao estado atual do app.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/index.css
git commit -m "feat: add dark mode color tokens to index.css"
```

---

### Task 2: `ThemeContext` + wiring no `App.tsx`

**Files:**
- Create: `frontend/src/context/ThemeContext.tsx`
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: nenhuma (usa só `localStorage` e `document.documentElement`, e o token
  `[data-theme="dark"]` da Task 1).
- Produces (usado pela Task 3): `useTheme(): { theme: "light" | "dark", toggleTheme: () => void }`
  e `ThemeProvider` de `frontend/src/context/ThemeContext.tsx`.

- [ ] **Step 1: Criar `frontend/src/context/ThemeContext.tsx`**

```tsx
import { createContext, useContext, useState, useEffect, useCallback, type ReactNode } from "react";

type Theme = "light" | "dark";

interface ThemeContextValue {
  theme: Theme;
  toggleTheme: () => void;
}

const THEME_KEY = "saas_theme";

function loadStoredTheme(): Theme {
  return localStorage.getItem(THEME_KEY) === "dark" ? "dark" : "light";
}

const ThemeContext = createContext<ThemeContextValue | null>(null);

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setTheme] = useState<Theme>(loadStoredTheme);

  useEffect(() => {
    document.documentElement.dataset.theme = theme;
    localStorage.setItem(THEME_KEY, theme);
  }, [theme]);

  const toggleTheme = useCallback(() => {
    setTheme((t) => (t === "light" ? "dark" : "light"));
  }, []);

  return <ThemeContext.Provider value={{ theme, toggleTheme }}>{children}</ThemeContext.Provider>;
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme deve ser usado dentro de <ThemeProvider>");
  return ctx;
}
```

- [ ] **Step 2: Envolver o app com `ThemeProvider` em `frontend/src/App.tsx`**

Substituir todo o conteúdo de `frontend/src/App.tsx` por:

```tsx
import { Navigate, Route, Routes } from "react-router-dom";
import { Toaster } from "react-hot-toast";
import { ThemeProvider } from "./context/ThemeContext";
import { AuthProvider, useAuth } from "./context/AuthContext";
import { ProtectedRoute } from "./components/ProtectedRoute";
import { Navbar } from "./components/Navbar";
import { LoginPage } from "./pages/LoginPage";
import { RegisterPage } from "./pages/RegisterPage";
import { DashboardPage } from "./pages/DashboardPage";
import { ChatPage } from "./pages/ChatPage";

function AppRoutes() {
  const { isAuthenticated } = useAuth();

  return (
    <>
      <Navbar />
      <Routes>
        <Route
          path="/login"
          element={isAuthenticated ? <Navigate to="/dashboard" replace /> : <LoginPage />}
        />
        <Route
          path="/register"
          element={isAuthenticated ? <Navigate to="/dashboard" replace /> : <RegisterPage />}
        />
        <Route element={<ProtectedRoute />}>
          <Route path="/dashboard" element={<DashboardPage />} />
          <Route path="/chat" element={<ChatPage />} />
        </Route>
        <Route path="*" element={<Navigate to={isAuthenticated ? "/dashboard" : "/login"} replace />} />
      </Routes>
    </>
  );
}

export default function App() {
  return (
    <ThemeProvider>
      <AuthProvider>
        <Toaster position="top-right" toastOptions={{ style: { fontFamily: "IBM Plex Sans, sans-serif" } }} />
        <AppRoutes />
      </AuthProvider>
    </ThemeProvider>
  );
}
```

(A única mudança real é a importação de `ThemeProvider` e envolver `<AuthProvider>` com ele — o
resto do arquivo é idêntico ao atual.)

- [ ] **Step 3: Verificar compilação**

```bash
cd frontend
npx tsc -b --noEmit
```

Esperado: nenhuma saída.

- [ ] **Step 4: Verificar manualmente**

1. `npm run dev`, abrir o app, abrir o console do DevTools.
2. Rodar `localStorage.getItem('saas_theme')` → esperado `null` (ainda não foi setado por
   nenhuma interação, mas o app já rodou o `useEffect` inicial) — na verdade, como o `useEffect`
   roda no mount e já persiste o valor default, o esperado é a string `"light"`.
3. Confirmar no DevTools (aba Elements) que `<html data-theme="light">` está presente.
4. Nenhuma mudança visual esperada ainda (o toggle só é adicionado na Task 3).

- [ ] **Step 5: Commit**

```bash
git add frontend/src/context/ThemeContext.tsx frontend/src/App.tsx
git commit -m "feat: add ThemeContext and wire into App"
```

---

### Task 3: Botão de toggle na Navbar

**Files:**
- Modify: `frontend/src/components/Navbar.tsx`

**Interfaces:**
- Consumes: `useTheme()` de `../context/ThemeContext` (Task 2).

- [ ] **Step 1: Substituir o conteúdo de `frontend/src/components/Navbar.tsx`**

```tsx
import { NavLink } from "react-router-dom";
import { Moon, Sun } from "lucide-react";
import { useAuth } from "../context/AuthContext";
import { useTheme } from "../context/ThemeContext";
import { RoleBadge } from "./RoleBadge";

const tabClass = ({ isActive }: { isActive: boolean }) =>
  "rounded-md px-3 py-1.5 text-sm font-medium transition-colors " +
  (isActive ? "bg-white/15 text-white" : "text-white/70 hover:text-white hover:bg-white/10");

export function Navbar() {
  const { session, logout } = useAuth();
  const { theme, toggleTheme } = useTheme();
  if (!session) return null;

  return (
    <header className="sticky top-0 z-10 bg-primary-dark shadow-sm">
      <div className="mx-auto flex max-w-6xl items-center justify-between gap-4 px-4 py-3 sm:px-6">
        <div className="flex items-center gap-6">
          <span className="font-display text-lg font-semibold tracking-tight text-white">
            Catálogo<span className="text-accent">.</span>
          </span>
          <nav className="flex items-center gap-1">
            <NavLink to="/dashboard" className={tabClass}>
              Produtos
            </NavLink>
            <NavLink to="/chat" className={tabClass}>
              Chat
            </NavLink>
          </nav>
        </div>

        <div className="flex items-center gap-3">
          <div className="hidden text-right sm:block">
            <p className="text-sm font-medium text-white">{session.company.name}</p>
            <p className="text-xs text-white/60">{session.user.name}</p>
          </div>
          <RoleBadge role={session.user.role} />
          <button
            onClick={toggleTheme}
            aria-label={theme === "light" ? "Ativar tema escuro" : "Ativar tema claro"}
            className="rounded-md border border-white/20 p-1.5 text-white/80 transition-colors hover:bg-white/10 hover:text-white"
          >
            {theme === "light" ? <Moon size={16} /> : <Sun size={16} />}
          </button>
          <button
            onClick={logout}
            className="rounded-md border border-white/20 px-3 py-1.5 text-sm text-white/80 transition-colors hover:bg-white/10 hover:text-white"
          >
            Sair
          </button>
        </div>
      </div>
    </header>
  );
}
```

(Única mudança: import de `Moon`/`Sun` e `useTheme`, e o novo `<button>` de toggle entre o
`RoleBadge` e o botão "Sair".)

- [ ] **Step 2: Verificar compilação**

```bash
cd frontend
npx tsc -b --noEmit
```

Esperado: nenhuma saída.

- [ ] **Step 3: Verificar manualmente**

1. `npm run dev`, logar no app.
2. Confirmar que o ícone de lua aparece na Navbar (tema claro ativo).
3. Clicar no botão → confirmar que `<html data-theme="dark">` no DevTools, e que o ícone vira sol.
4. Clicar de novo → volta pra lua e `data-theme="light"`.
5. Recarregar a página com o tema escuro ativo → confirmar que o app abre já em `data-theme="dark"`
   (persistência funcionando), mesmo sem nenhum componente ainda estar visualmente ajustado pro
   escuro (isso é esperado até a Task 4).

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/Navbar.tsx
git commit -m "feat: add theme toggle button to Navbar"
```

---

### Task 4: Ajustar componentes existentes pros tokens de tema escuro

**Files:**
- Modify: `frontend/src/pages/LoginPage.tsx`
- Modify: `frontend/src/pages/RegisterPage.tsx`
- Modify: `frontend/src/components/ProductCard.tsx`
- Modify: `frontend/src/components/ProductFormModal.tsx`
- Modify: `frontend/src/components/ChatBubble.tsx`
- Modify: `frontend/src/pages/ChatPage.tsx`
- Modify: `frontend/src/components/RoleBadge.tsx`

**Interfaces:** nenhuma — só troca de classes Tailwind, sem mudança de props/assinaturas.

Esta task substitui a classe `bg-white` (Tailwind puro, hardcoded) pela nossa variável de tema
`bg-surface`, e as classes `text-primary-dark`/`text-accent-dark` usadas como *cor de texto sobre
fundo soft* pelas novas `text-primary-soft-text`/`text-accent-soft-text` (Task 1). Cada passo abaixo
lista a string exata a trocar — são strings literais de `className`, não regex.

- [ ] **Step 1: `frontend/src/pages/LoginPage.tsx` — trocar `bg-white` por `bg-surface`**

O arquivo tem exatamente 2 ocorrências da string `bg-white`, ambas dentro do mesmo `className`
literal (inputs de email e senha):
```
"rounded-lg border border-line bg-white px-3.5 py-2.5 text-ink outline-none transition-colors focus:border-primary"
```
Trocar `bg-white` por `bg-surface` nas duas ocorrências (substituição direta da substring, sem
outras ocorrências de `bg-white` no arquivo).

- [ ] **Step 2: `frontend/src/pages/RegisterPage.tsx` — trocar as 7 ocorrências de `bg-white`**

Duas no toggle de modo (strings diferentes uma da outra, cada uma só tem 1 ocorrência de `bg-white`):
```
(mode === "new-company" ? "bg-white text-ink shadow-sm" : "text-ink-soft")
(mode === "join-company" ? "bg-white text-ink shadow-sm" : "text-ink-soft")
```
Trocar `bg-white` por `bg-surface` nas duas.

Cinco nos inputs (nome, email, senha, nome da empresa, ID da empresa) — todas usam a substring
`bg-white` dentro de className de input, sem nenhuma outra ocorrência de `bg-white` no arquivo além
dessas 7 no total. Trocar `bg-white` por `bg-surface` em todas.

- [ ] **Step 3: `frontend/src/components/ProductCard.tsx` — trocar só o `bg-white` do card, não o `bg-white/70`**

**Atenção:** este arquivo tem duas ocorrências da substring `bg-white` — uma é o fundo do card
(que deve virar `bg-surface`), a outra é `bg-white/70`, o "furinho" decorativo da etiqueta de
preço, que **deve continuar branco literal em qualquer tema** (é um pontinho branco sobre a tag
laranja, não um "fundo de superfície"). Não faça um replace-all ingênuo de `bg-white` nesse
arquivo — ele bateria nas duas ocorrências.

Troca 1 — o container do card:
```
old: className="group flex flex-col overflow-hidden rounded-xl border border-line bg-white shadow-sm transition-shadow hover:shadow-md"
new: className="group flex flex-col overflow-hidden rounded-xl border border-line bg-surface shadow-sm transition-shadow hover:shadow-md"
```

Troca 2 — a tag de categoria, texto sobre fundo soft:
```
old: className="w-fit rounded-full bg-primary-soft px-2 py-0.5 text-xs font-medium text-primary-dark"
new: className="w-fit rounded-full bg-primary-soft px-2 py-0.5 text-xs font-medium text-primary-soft-text"
```

A linha `<span className="absolute top-1/2 left-2 h-1 w-1 -translate-y-1/2 rounded-full bg-white/70" />`
**não muda**.

- [ ] **Step 4: `frontend/src/components/ProductFormModal.tsx` — 2 trocas**

Troca 1 — fundo do modal:
```
old: className="w-full max-w-md rounded-xl bg-white p-6 shadow-xl"
new: className="w-full max-w-md rounded-xl bg-surface p-6 shadow-xl"
```

Troca 2 — texto do botão de escolher arquivo, sobre fundo soft (dentro do `file:` variant):
```
old: className="flex-1 text-sm text-ink-soft file:mr-3 file:rounded-lg file:border-0 file:bg-primary-soft file:px-3 file:py-1.5 file:text-sm file:font-medium file:text-primary-dark hover:file:bg-primary-soft/80"
new: className="flex-1 text-sm text-ink-soft file:mr-3 file:rounded-lg file:border-0 file:bg-primary-soft file:px-3 file:py-1.5 file:text-sm file:font-medium file:text-primary-soft-text hover:file:bg-primary-soft/80"
```

- [ ] **Step 5: `frontend/src/components/ChatBubble.tsx` — trocar `bg-white` por `bg-surface`**

Única ocorrência, no branch do assistente:
```
old: "rounded-bl-sm border border-line bg-white text-ink"
new: "rounded-bl-sm border border-line bg-surface text-ink"
```

- [ ] **Step 6: `frontend/src/pages/ChatPage.tsx` — trocar `bg-white` por `bg-surface`**

Única ocorrência, no input de mensagem:
```
old: className="flex-1 rounded-lg border border-line bg-white px-4 py-2.5 text-ink outline-none focus:border-primary"
new: className="flex-1 rounded-lg border border-line bg-surface px-4 py-2.5 text-ink outline-none focus:border-primary"
```

- [ ] **Step 7: `frontend/src/components/RoleBadge.tsx` — trocar os dois tokens de texto-sobre-soft**

```
old: (isAdmin ? "bg-accent-soft text-accent-dark" : "bg-primary-soft text-primary-dark")
new: (isAdmin ? "bg-accent-soft text-accent-soft-text" : "bg-primary-soft text-primary-soft-text")
```

- [ ] **Step 8: Verificar compilação**

```bash
cd frontend
npx tsc -b --noEmit
```

Esperado: nenhuma saída.

- [ ] **Step 9: Verificar manualmente que nada mudou no tema claro**

`npm run dev`, com o tema em claro (`data-theme="light"` ou nenhum atributo), navegar por
Login, Register, Dashboard (grid de produtos + modal de novo/editar produto) e Chat. Como
`--color-surface` e as duas variáveis `-soft-text` valem exatamente o mesmo que `white`/`-dark`
valiam antes no tema claro, a expectativa é **zero mudança visual** — é uma checagem de
regressão, não de uma feature nova.

- [ ] **Step 10: Commit**

```bash
git add frontend/src/pages/LoginPage.tsx frontend/src/pages/RegisterPage.tsx frontend/src/components/ProductCard.tsx frontend/src/components/ProductFormModal.tsx frontend/src/components/ChatBubble.tsx frontend/src/pages/ChatPage.tsx frontend/src/components/RoleBadge.tsx
git commit -m "fix: replace hardcoded bg-white and dual-role text tokens for dark mode"
```

---

### Task 5: Verificação end-to-end do tema escuro

**Files:** nenhum (task só de verificação — se algo falhar aqui, o problema está em alguma das
tasks anteriores e deve ser corrigido nelas, não aqui).

- [ ] **Step 1: Fluxo completo no navegador**

Com backend e frontend rodando (`npm run dev` nos dois), e logado como algum admin do seed
(`admin@techstore.com` / `senha123`):

1. Confirmar tema claro por padrão (sem preferência salva antes).
2. Clicar no toggle da Navbar → tema escuro ativa.
3. **Dashboard:** cards de produto com fundo escuro (`--color-surface` escuro) e texto claro
   legível; tag de categoria legível (fundo verde escuro, texto verde claro); etiqueta de preço
   (laranja) continua legível — ela não depende de nenhum token trocado nesta feature.
4. Abrir o modal de "Novo produto" (ou editar um existente) → modal com fundo escuro, inputs
   legíveis, label do botão de arquivo legível sobre o fundo "soft" escuro.
5. **Navbar:** badge de role (ADMIN/USUÁRIO) legível nos dois temas.
6. **Chat:** bolha do assistente com fundo escuro e texto claro; input de mensagem com fundo
   escuro.
7. Navegar para `/login` (deslogar antes, ou abrir em aba anônima após copiar o localStorage) →
   confirmar que a tela de login também aparece no tema escuro (persistência global via
   `data-theme` no `<html>`), mesmo sem ter um botão de toggle ali.
8. Recarregar a página (F5) com o tema escuro ativo → confirmar que continua escuro após reload.
9. Alternar de volta pro claro e confirmar que tudo volta ao visual original.

- [ ] **Step 2: Regressão final de compilação**

```bash
cd frontend && npx tsc -b --noEmit
cd ../backend && npx tsc -p tsconfig.json --noEmit
```

Esperado: nenhuma saída em nenhum dos dois (esta feature não toca o backend, mas confirma que o
repositório como um todo continua compilando).

---

## Self-Review

**Cobertura do spec:** todas as seções do spec
(`docs/superpowers/specs/2026-07-15-dark-mode-design.md`) têm task correspondente — paleta CSS
(Task 1), contexto/persistência (Task 2), toggle na UI (Task 3), correção dos componentes
existentes (Task 4), verificação end-to-end (Task 5).

**Placeholders:** nenhum "TBD"/"implementar depois" — todo código e toda troca de string estão
completos e explícitos em cada step, incluindo a ressalva explícita sobre `bg-white/70` no
`ProductCard.tsx` pra evitar um replace-all incorreto.

**Consistência de tipos:** `useTheme()` (Task 2) é consumido com o mesmo nome e formato de retorno
(`{ theme, toggleTheme }`) na Task 3; os tokens `--color-surface`, `--color-primary-soft-text`,
`--color-accent-soft-text` definidos na Task 1 são exatamente os nomes usados nas classes Tailwind
da Task 4 (`bg-surface`, `text-primary-soft-text`, `text-accent-soft-text`).
